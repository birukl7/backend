# Port (Render sets PORT automatically — do not hardcode in production)
AI_SERVICE_PORT=8001

# Comma-separated origins allowed to call this API (your Laravel app URL)
ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000,https://backend-q9yx.onrender.com
